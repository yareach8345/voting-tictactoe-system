spring:
  r2dbc:
    url: r2dbc:h2:mem:///test;MODE=MySQL;DATABASE_TO_UPPER=FALSE
    username: sa
  sql:
    init:
      mode: always
      schema-locations: sql/test-schema-init.sql
logging:
  level:
    org.springframework.r2dbc.core: debug